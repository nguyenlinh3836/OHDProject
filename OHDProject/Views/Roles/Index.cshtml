@model IEnumerable<OHDProject.Models.Role>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoleId)
            </th>  <th>
                @Html.DisplayNameFor(model => model.RoleName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoleId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoleId" class="btn btn-primary">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoleId" class="btn btn-success">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoleId" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
